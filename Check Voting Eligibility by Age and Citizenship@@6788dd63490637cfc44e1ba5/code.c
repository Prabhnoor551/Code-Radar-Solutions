#include <stdio.h>
int main() 
{
    int age,b;

    scanf("%d",&age);
    scanf("%d",&b);

    if(a==1)
    {
        if(a>=18)
        {
        printf("Eligible");
        }
        else{
            printf("Not Eligible");
        }
    }
    else{
        printf("Not Eligible");

    }

    return 0;
}